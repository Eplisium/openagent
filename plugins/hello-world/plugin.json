{
  "name": "hello-world",
  "version": "1.0.0",
  "description": "A simple hello world plugin demonstrating the OpenAgent plugin API",
  "author": "OpenAgent",
  "license": "MIT",
  "main": "index.js",
  "tools": [
    {
      "name": "hello_greet",
      "description": "Greet a user with a friendly message",
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Name of the person to greet"
          }
        },
        "required": ["name"]
      }
    },
    {
      "name": "hello_echo",
      "description": "Echo back a message with a prefix",
      "parameters": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string",
            "description": "Message to echo"
          },
          "prefix": {
            "type": "string",
            "description": "Prefix to add to the message"
          }
        },
        "required": ["message"]
      }
    }
  ],
  "hooks": {
    "onAgentStart": "onAgentStart",
    "onTaskComplete": "onTaskComplete"
  }
}